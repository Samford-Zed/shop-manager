<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Development: allow cleartext HTTP to any host.
         Do NOT ship this in production; switch to HTTPS and a stricter config. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
